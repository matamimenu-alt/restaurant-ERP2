[build]
builder = "nixpacks"
buildCommand = "npm run install:all && npm run db:generate && npm run build"

[deploy]
startCommand = "node /app/backend/dist/src/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[healthcheck]
path = "/health"
timeout = 30
