[build]
builder = "nixpacks"
buildCommand = "cd backend && npm install && npm run db:generate && npm run build"

[deploy]
startCommand = "cd backend && npm run start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[healthcheck]
path = "/health"
timeout = 30
